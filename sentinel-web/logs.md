give the workflow.yml GET /contents/.github/workflows/file.yml
, commit diff '  GET /commits/{sha}
, crooped logs    GET /commits/{sha}
, step details    GET /runs/{run_id}/jobs





onething can be implemented that the service are seen that has the data of previous 14 days and shown the latenfcy uptime error and all 
we can do one thing we can fetch the data in realtime so that error is got and incident is created at that time 